{
  "name": "mcp-apps-quiz",
  "version": "0.1.0",
  "private": true,
  "type": "module",
  "packageManager": "pnpm@10.29.3",
  "scripts": {
    "build": "pnpm -r build",
    "dev": "pnpm -F @quiz/server dev",
    "test": "pnpm -r test",
    "lint": "biome check .",
    "format": "biome format --write .",
    "codegen": "pnpm -F @quiz/server codegen"
  },
  "devDependencies": {
    "@biomejs/biome": "^1.9.4",
    "typescript": "^5.9.3"
  },
  "pnpm": {
    "onlyBuiltDependencies": ["better-sqlite3", "esbuild", "@biomejs/biome"]
  }
}
